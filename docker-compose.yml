# ----------------------------------------------------------------------------------------

# docker-compose.yml for MoodMap
# ______________________________
#
# Created by: Guacamoleboy / Jonas Meinert Larsen
# E-mail: cph-jl325@stud.ek.dk
#
# Last tested: 06/04-2026
# Last updated: 06/04-2026
#
# URL Link: https://www.moodmap.guacamoleboy.dk

# ----------------------------------------------------------------------------------------

# ________________________________________________________________________________________

services:

  # --------------------------------------------------------------------------------------
  # Setup
  db:
    image: postgres:16
    container_name: ${DB_HOST}
    restart: always
    ports:
      - ${DB_EXPOSED_PORTS:-5432}
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER}"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s

  # --------------------------------------------------------------------------------------
  # PgAdmin

  pgadmin:
    image: dpage/pgadmin4
    container_name: ${PGADMIN_CONTAINER_NAME}
    restart: always
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_DEFAULT_EMAIL}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_DEFAULT_PASSWORD}
    ports:
      - ${PGADMIN_PORTS}
    depends_on:
      db:
        condition: service_healthy

  # --------------------------------------------------------------------------------------
  # REST API Setup
  api:
    image: guacamoleboy/moodmap-api:latest
    container_name: ${DB_CONTAINER_NAME}
    restart: always
    ports:
      - ${DB_PORTS}
    environment:
      - DB_NAME=${DB_NAME}
      - DB_USERNAME=${DB_USERNAME}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_HOST=${DB_HOST}
      - SERVER_PORT=${SERVER_PORT}
      - API_BASE_PATH=${API_BASE_PATH}
      - URL_PATHING=${URL_PATHING}
      - ROUTE_ENDPOINT_FOLDER=${ROUTE_ENDPOINT_FOLDER}
    depends_on:
      - db
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:${SERVER_PORT}${API_BASE_PATH}/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

    # --------------------------------------------------------------------------------------
    # Watchtower

  watchtower:
    image: containrrr/watchtower:latest
    container_name: watchtower
    restart: always
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - WATCHTOWER_CLEANUP=true
      - WATCHTOWER_POLL_INTERVAL=300
    command: ${DB_CONTAINER_NAME}
    depends_on:
      api:
        condition: service_healthy

# ________________________________________________________________________________________

volumes:
  postgres_data: